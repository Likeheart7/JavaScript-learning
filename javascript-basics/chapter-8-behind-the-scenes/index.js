'use strict';
/*
function first() {
  console.log(flag); //undefined
  if (10 > 30) {
    const num = 30;
    var flag = true; // var声明的变量是function scope
  }
  console.log(flag); // undefined
  console.log(num); // index.js:9 Uncaught ReferenceError: num is not defined
}

first();
*/

// function
/*
console.log(addDecl(2, 3)); //5
console.log(addExpr(2, 3)); // Uncaught ReferenceError: Cannot access 'addExpr' before initialization
console.log(addArrow(2, 3)); // Uncaught ReferenceError: Cannot access 'addArrow' before initialization
function addDecl(a, b) {
  return a + b;
}
const addExpr = function (a, b) {
  return a + b;
};

const addArrow = (a, b) => {
  return a + b;
};
 */
